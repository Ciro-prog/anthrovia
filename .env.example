# Solo el front (Vercel → Environment Variables, o .env local). No va en cms/.env del VPS.
# Sin esto: contenido local /ethos y Live Preview no recibe el borrador.
# CMS_URL=http://localhost:60518
CMS_URL=http://pampaservers.com:60518
# Local: también sirve VITE_CMS_URL (no la uses en Vercel).
# VITE_CMS_URL=http://localhost:60518

# Webhook postulaciones (n8n)
# VITE_WEBHOOK_URL=
